import 'package:flutter/material.dart';

class ProductSalad extends StatelessWidget {
  const ProductSalad({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
