import 'package:flutter/material.dart';

class ProductPizza extends StatelessWidget {
  const ProductPizza({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
